{% extends 'base.html.twig' %}

{% block title %}Album{% endblock %}

{% block body %}
    <h1>Album</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ album.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ album.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ album.description }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ album.date }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('album_base_index') }}">back to list</a>

    <a href="{{ path('album_base_edit', {'id': album.id}) }}">edit</a>

    {{ include('album_base/_delete_form.html.twig') }}
{% endblock %}
