<form method="post" action="{{ path('socialnet_base_delete', {'id': socialnet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ socialnet.id) }}">
    <button class="btn btn-primary boton">Delete</button>
</form>
